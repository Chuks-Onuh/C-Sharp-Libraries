using System;

namespace Week2Task
{
    class Program
    {
        static void Main(string[] args)
        {
            //GeneralClass<int> linklist = new GeneralClass   <int>();
            //// adding element(s) to the list
            //linklist.AddToTail(5);
            //linklist.AddToTail(2);
            //linklist.AddToTail(1);
            //Console.Write("Element(s) in the List is/are: ");
            //linklist.PrintList(linklist);
            //Console.WriteLine();

            // removing an element in the list and return true if exist and -1 if not in the list 

            //Console.WriteLine($"Removing element: {linklist.Remove(11)}");
            //// checking if element exist in the list.
            //Console.WriteLine($"Checking element: {linklist.Check(12)}");
            //// checking the index of an element in the list
            //Console.WriteLine($"Index value: {linklist.Index(12)}");

            //Queue<int> queue = new Queue<int>();
            //adding element(s) to the list
            //queue.Enqueue(6);
            //queue.Enqueue(4);
            //queue.Enqueue(8);
            //Console.Write("Element(s) in the Queue is/are: ");
            //queue.PrintList(queue);
            //Console.WriteLine();

            //// removing an element in the list and return true if exist and - 1 if not in the Queue
            //try
            //{
            //    queue.Dequeue();
            //}
            //catch (Exception)
            //{

            //    Console.WriteLine("The queue is empty");
            //}
            // checking size of elements in the queue
            //Console.WriteLine(queue.Size());

            // checking if queue is empty or not
            //Console.WriteLine(queue.IsEmpty());

            //Stack<int> stack = new Stack<int>();
            // adding element(s) to the Stack
            //stack.Push(2);
            //stack.Push(11);
            //stack.Push(12);
            //Console.Write("Element(s) in the Stack is/are: ");
            //stack.PrintList(stack);
            //Console.WriteLine();

            // removing and returns the last item added to the stack
            //stack.Pop();
            //stack.PrintList(stack);

            // returns the last item added to the stack
            //try
            //{
            //    stack.Peek();
            //}
            //catch (Exception)
            //{

            //    Console.WriteLine("No element in the stack");
            //}
           // number of items currently in the stack
            //stack.Size();
            // returns true if the stack is empty and false is it isn’t
            //Console.WriteLine(stack.IsEmpty());
        }
    }
}
